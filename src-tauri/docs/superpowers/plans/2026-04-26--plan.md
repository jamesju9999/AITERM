```markdown
# SwiftRef API Java 呼叫範例 - 實作計畫

## 階段 1: 專案初始化
**目標**: 建立專案結構與基礎設定

### 任務 1.1: 建立 Maven 專案
- [ ] 建立專案目錄結構
- [ ] 建立 pom.xml 檔案
- [ ] 設定 Java 編譯版本為 11+
- [ ] 加入必要依賴：
  - Jackson (JSON 處理)
  - SLF4J + Logback (日誌)

### 任務 1.2: 建立設定檔範本
- [ ] 建立 `config.properties.example`
- [ ] 定義必要設定項目：
  - `swift.consumer.key`
  - `swift.consumer.secret`
  - `swift.api.base.url`
  - `swift.oauth.token.endpoint`

### 任務 1.3: 建立專案文件
- [ ] 建立 README.md（說明如何使用）
- [ ] 建立 .gitignore（排除 config.properties）

**預估時間**: 30 分鐘

---

## 階段 2: 實作設定管理模組
**目標**: 實作 ConfigManager.java

### 任務 2.1: 實作 ConfigManager 類別
- [ ] 建立 `ConfigManager.java`
- [ ] 實作 Properties 載入邏輯
- [ ] 實作環境變數覆寫機制
- [ ] 加入設定值驗證（檢查必要項目是否存在）

### 任務 2.2: 實作存取方法
- [ ] `getConsumerKey()`
- [ ] `getConsumerSecret()`
- [ ] `getApiBaseUrl()`
- [ ] `getTokenEndpoint()`

### 任務 2.3: 錯誤處理
- [ ] 處理設定檔不存在的情況
- [ ] 處理必要設定項目缺失的情況

**預估時間**: 45 分鐘

---

## 階段 3: 實作認證服務
**目標**: 實作 AuthService.java

### 任務 3.1: 實作 OAuth 認證邏輯
- [ ] 建立 `AuthService.java`
- [ ] 注入 ConfigManager 依賴
- [ ] 建立 HttpClient 實例

### 任務 3.2: 實作 authenticate() 方法
- [ ] 建立 OAuth token 請求
  - 設定 Content-Type: application/x-www-form-urlencoded
  - 設定 grant_type=client_credentials
  - 加入 client_id 和 client_secret
- [ ] 發送 POST 請求到 token 端點
- [ ] 解析 JSON 回應取得 access_token

### 任務 3.3: 錯誤處理
- [ ] 處理 HTTP 401/403 錯誤
- [ ] 處理網路連線異常
- [ ] 處理 JSON 解析錯誤
- [ ] 加入適當的日誌記錄

**預估時間**: 1 小時

---

## 階段 4: 實作資料模型
**目標**: 實作 BicResponse.java

### 任務 4.1: 建立資料類別
- [ ] 建立 `model/BicResponse.java`
- [ ] 定義欄位對應 API 回應：
  - `bic`
  - `institutionName` (對應 `institution_name`)
  - `city`
  - `country`
  - 其他相關欄位

### 任務 4.2: 設定 Jackson 註解
- [ ] 使用 `@JsonProperty` 處理欄位名稱對應
- [ ] 使用 `@JsonIgnoreProperties(ignoreUnknown = true)` 忽略未知欄位

### 任務 4.3: 實作輔助方法
- [ ] 覆寫 `toString()` 方法（格式化輸出）
- [ ] 實作 Getter/Setter

**預估時間**: 30 分鐘

---

## 階段 5: 實作 API 客戶端
**目標**: 實作 SwiftRefClient.java

### 任務 5.1: 建立客戶端類別
- [ ] 建立 `SwiftRefClient.java`
- [ ] 注入 AuthService 和 ConfigManager
- [ ] 建立 HttpClient 和 ObjectMapper 實例

### 任務 5.2: 實作 queryBic() 方法
- [ ] 呼叫 AuthService 取得 access token
- [ ] 建立 GET 請求到 `/v1/bicplus/{bic}`
- [ ] 設定 Authorization header: `Bearer {token}`
- [ ] 發送請求並取得回應

### 任務 5.3: 實作回應處理
- [ ] 檢查 HTTP 狀態碼
- [ ] 將 JSON 回應反序列化為 BicResponse 物件
- [ ] 回傳結果

### 任務 5.4: 錯誤處理
- [ ] 處理 HTTP 404（BIC 不存在）
- [ ] 處理 HTTP 400（無效的 BIC 格式）
- [ ] 處理其他 HTTP 錯誤
- [ ] 加入適當的日誌記錄

**預估時間**: 1.5 小時

---

## 階段 6: 實作應用程式入口
**目標**: 實作 Main.java

### 任務 6.1: 建立主程式類別
- [ ] 建立 `Main.java` 與 `main()` 方法
- [ ] 初始化 ConfigManager
- [ ] 初始化 AuthService
- [ ] 初始化 SwiftRefClient

### 任務 6.2: 實作命令列參數處理
- [ ] 檢查參數數量
- [ ] 無參數時使用預設 BIC（例如：CHASUS33）
- [ ] 有參數時使用第一個參數作為 BIC

### 任務 6.3: 實作 API 呼叫流程
- [ ] 呼叫 `swiftRefClient.queryBic(bic)`
- [ ] 顯示查詢結果（使用 BicResponse.toString()）

### 任務 6.4: 實作錯誤處理
- [ ] 使用 try-catch 捕捉所有例外
- [ ] 顯示友善的錯誤訊息
- [ ] 確保程式正常結束

**預估時間**: 45 分鐘

---

## 階段 7: 整合測試與優化
**目標**: 驗證功能並優化程式碼

### 任務 7.1: 功能測試
- [ ] 使用有效憑證測試認證流程
- [ ] 測試查詢已知的 BIC 碼
- [ ] 測試無效的 BIC 碼
- [ ] 測試無效的憑證

### 任務 7.2: 錯誤情境測試
- [ ] 測試網路斷線情況
- [ ] 測試設