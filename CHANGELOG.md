# 變更記錄

這裡的文字會被 `.github/workflows/release.yml` 直接放進 GitHub Release 的
「更新項目」。**寫給使用者看，不是寫給開發者看**——講他們會遇到什麼、變成
怎樣，不要講模組名稱或函式名稱。

發版前在最上面新增一段 `## v<版本>`，內容就是那一版的更新項目。沒有對應段落
時，workflow 會退回用 commit 標題產生草稿（那份草稿長得像開發者日誌，通常需要
手動改寫）。

## v1.17.0

**遠端終端機的 Ask AI 升級成完整的 AI 對話面板**
之前遠端終端機分頁的 Ask AI 只是一顆陽春的控制列。現在點開會是跟本機終端機一樣的完整 AI 面板：可以自由對話問問題、切換要用哪個 AI 供應商、瀏覽並繼續之前的對話紀錄。AI 用的是**你自己**設定的供應商，不需要對方那台電腦有設定 AI。

**AI 代理可以自己動手操作對方的電腦**
在遠端終端機分頁打 `/agent 目標`，AI 會自己規劃、下指令、看執行結果，一步一步接續下去直到完成，每條指令都會照樣整理成卡片顯示在畫面上，過程隨時可以按「停止」立刻中止。連線中途重新同步、對方收回操作權、或連線中斷時，面板會清楚告訴你 AI 為什麼停下來，不會沒頭沒尾地消失；等操作權拿回來後，一樣可以再次啟動代理，不會卡住。

## v1.16.0

**新功能：把終端機分頁即時分享給同事看，或讓對方直接操作**
點終端機分頁工具列的「分享」按鈕，會產生一組四位數字驗證碼；把驗證碼跟連線位址告訴同事，對方在自己的 AITerm 用「連線到同事分享的終端機」輸入後，雙方各自唸出算出來的驗證碼互相核對（確保沒有被中間人冒充），核對過就能即時看到你的終端機畫面。你可以選擇讓對方唯讀（只能看）或授權操作（可以直接打字），也能隨時收回。對方直接在畫面上打的指令、或透過下方輸入框送出的指令，都會跟你自己打的一樣整理成一張一張的指令卡片，方便回頭查內容。

**遠端終端機分頁的畫面體驗貼近本機**
即時畫面的高度改成動態調整——閒置時只留提示字元那麼高，指令執行中才會撐開，不再固定佔用一大片空白。全螢幕程式（例如 vim、htop、或 Claude Code CLI）也會正常撐滿畫面、不會被裁掉看不到。新指令完成時會自動捲到最新的一張卡片，不用自己往下滑。

**遠端終端機新增工具列：可以用指令書籤、直接看到連線資訊，還能換連別台主機**
畫面上方會顯示對方的位址、連線狀態、已經連線多久；新增「指令書籤」按鈕，可以直接叫出常用指令清單插入輸入框；新增「連線」按鈕，不用關掉這個分頁就能改連到另一台分享出來的終端機。

**修正對方在你連線之後調整終端機視窗大小，畫面可能出現文字重疊**
如果你連進去之後，對方才調整了終端機視窗的大小或切換全螢幕，先前有機率讓畫面顯示錯位、文字疊在一起，尤其容易發生在 Claude Code CLI 這類會整頁重繪內容的程式上。現在對方一調整大小就會立刻通知你，畫面不會再對不上。

## v1.15.0

**Agent 指令卡住時不再整個停住不動**
以前如果 AI 產生的指令讓終端機停在等輸入的狀態（例如一段沒有正確收尾的多行文字），Agent 就會一直停在「執行指令中」，畫面上什麼都不會發生，只能自己按停止鈕結束整個任務。現在遇到這種情況會跳出提示，你可以選「繼續等待」，或選「中斷並繼續」——後者會幫你把終端機拉回正常狀態，並讓 AI 帶著目前已經拿到的結果接著做下去，不用整個任務重來。

判斷的依據是「這個指令有多久完全沒有動靜」，不是「跑了多久」，所以像弱掃、建置這類本來就要跑很久、但會持續輸出訊息的工作不會被誤判打斷。

## v1.14.0

**關閉分頁前會先問一聲，不會再誤關掉正在進行的工作**
以前不小心按到分頁的 ✕，正在跑的指令、正在執行的 Agent 任務、還沒存下來的 AI 對話就這樣沒了，連個提示都沒有。現在遇到這些情況會先跳出確認框，讓你決定要繼續還是關掉；空白或閒置的分頁照舊一按就關，不會多問一句。另外，對「不是正在看的那個」分頁按 ✕ 時，畫面會先自動切過去再問——以前這種情況確認框會出現在看不見的地方，結果那個分頁怎麼按都關不掉。

**修正使用部分本地模型時，AI 功能常跳出「回傳格式錯誤」而無法使用**
用某些自己架設的本地模型時，版本控制的 AI 助手和 `/ai` 產生指令會不時失敗，跳出「AI 回傳格式錯誤」或「AI 無法規劃下一步」，而且每次的錯誤內容都不太一樣。原因是先前只要求模型「回一份 JSON」，卻沒有規定那份 JSON 該長什麼樣子，模型有時就會回一份格式合法、內容卻對不上的資料。現在會把需要的欄位一併告訴模型伺服器，由它在生成當下就限制住，這類錯誤不會再發生。

**修正在設定頁面無法拖動視窗**
切到設定頁之後，不管抓哪裡都拖不動視窗，得先按「回到終端機」才移動得了。現在在設定頁側邊欄的空白處就可以直接拖動視窗。

## v1.13.0

**「找另一個 AI 幫忙」更快也更不容易卡住**
上一版新增的 agent 協調功能（開新分頁找另一個 AI 幫忙做事）這次修掉了幾個會讓它卡住不動的情況。新分頁如果被要求確認「是否信任這個資料夾」，現在會自動幫你按確認過關，不會像以前一樣卡在那個畫面、讓人以為它當機了；判斷「對方是不是已經做完」的邏輯也調整過，不會再像以前一樣，明明對方早就做完了，卻要白白多等好幾分鐘才發現。

## v1.12.0

**讓 Claude Code CLI 開新分頁、找另一個 AI 幫忙**
在「設定 → MCP 工具伺服器」新增一個獨立開關「啟用 agent 協調工具」（預設關閉）。開啟後，正在 AITerm 終端機裡工作的 Claude Code CLI 可以自己開一個新分頁、在裡面啟動另一個 AI（例如再開一個 Claude Code 或 Codex），把工作分給它、等它做完再繼續——例如一個負責寫程式、一個負責寫測試。新開出來的分頁會有綠色機器人圖示標示，滑鼠移上去會提示是 AI 自己開的。

**修正新分頁偶爾整個是黑的，要按 Enter 或拉一下視窗才會出現提示字元**
某些情況下開一個新的終端機分頁，畫面會先卡在全黑、沒有任何提示字元，必須手動按一下 Enter 或稍微調整視窗大小才會顯示出來。這次找到並修好了根本原因，新分頁現在會直接正常顯示。

## v1.11.0

**讓 Claude Code CLI 直接查你在 AITerm 設定的資料庫和知識庫**
新增「MCP 工具伺服器」功能（設定 → MCP 工具伺服器）。開啟後，Claude Code CLI（或其他支援 MCP 的工具）可以直接呼叫你在 AITerm 裡已經設定好的資料庫連線和知識庫，不用再幫每個工具重複設定一次連線資訊。啟用後頁面上會顯示一段註冊指令，複製貼到終端機跑一次就完成連接。

**Claude Code 橋接的下拉選單外觀調整**
「模型對應」那幾個下拉選單的樣式跟其他設定頁面對齊，看起來更一致。

## v1.10.0

**Word / PDF / PowerPoint 轉檔變快也變準，而且大多不用再裝 Python**
文件轉換器（以及知識庫同步）新增了一個內建的轉檔引擎，處理 Word、PDF、PowerPoint、Excel、CSV、EPUB 等常見格式時直接秒轉，不用再等第一次使用時安裝 Python 套件。碰到它不支援的格式（例如圖片、音檔、Outlook 信件、網頁）還是會自動改用原本的轉檔方式，不會轉不出來。想維持舊行為，可以到設定 → 一般 → 「文件轉換引擎」切回「只用 MarkItDown」。

**轉換文件時，畫面上會有個東西在動**
以前轉換中只有一行靜態文字，不確定是不是卡住了。現在會有個轉圈圖示提醒你還在處理。

**下載轉出的 Markdown 檔案，可以自己選存哪裡**
以前下載一律存到系統預設的下載資料夾。現在會跳出「另存新檔」視窗，可以自己選資料夾跟檔名。

**知識庫聊天畫面偶爾會整個往上跳，卡住不會恢復**
在某些情況下切到別的分頁再切回知識庫，整個 app 畫面會突然往上位移一段，切去別的分頁也不會恢復，只能重開視窗。這次修好了根本原因。

## v1.9.2

**知識庫同步後查不到內容**
修好幾個會讓已同步的文件在對話中搜尋不到的問題。文件轉檔後沒有任何文字（例如純掃描圖片 PDF）以前會被誤判成「同步成功」，現在會正確標記失敗並提示原因；內容很大的文件（例如很長的報告）以前可能因為一次把全部內容送去向量化而逾時，現在會分批處理；同時同步多本筆記本時，畫面上的同步進度以前偶爾會互相干擾，現在各自獨立顯示。也調整了文件切片的大小，讓沒有明確段落標題的長文件（如掃描報告）語意搜尋更準確。

## v1.9.1

**修正知識庫分頁畫面往上位移**
當左側筆記本清單或右側對話記錄塞滿很多項目後，切到別的分頁再切回知識庫，畫面偶爾會整個往上跳一段。現在修正了。

## v1.9.0

**開啟 AITerm 先看到首頁**
以前一打開就直接落在上次的分頁裡。現在會先停在一個首頁，一眼看完今天要接手的東西：正在跑的自動任務進度、上次每個分頁在哪個目錄做到哪、最近工作過的專案、今日的 AI 用量。側邊欄最上面多了一顆「首頁」按鈕，隨時按它或 `Ctrl+0` 都能回來。上次的分頁照常還原，只是不在前景。

**打一句話，AI 幫你開對地方**
首頁最上面有個輸入框，直接用自然語言講你要做什麼，AI 會判斷該開終端機、資料庫、知識庫還是其他分頁。判斷錯了不用重來——新分頁上會有一條提示，可以直接換成別種，用同一句話重開。AI 沒設定或連不上時會退回開終端機並把整句話當成任務目標，輸入框永遠有反應。

**重開 AITerm 後，每個終端機回到自己上次的目錄**
以前所有分頁都會開在同一個目錄。現在各自記得自己在哪，重開就在原處繼續。若那個目錄已經被刪掉或改名，會退回家目錄而不是莫名其妙的位置。

**側邊欄的分頁可以拖曳排序**
按住往上或往下拖就能換位置，順序會記住。注意 `Ctrl+1~9` 是按目前順序對應的，重排之後跟著改變。

**自動任務的進度現在真的看得到**
以前只有企業派送的任務會顯示進度，自己打 `/agent` 跑的完全不會出現。現在四種啟動方式都會回報，而且任務結束（含中途打字中斷）會把進度清掉，不會永遠掛著一個跑完的任務。

**Telegram 遠端遙控不會再漏掉指令**
以前切到別的分頁、或按下首頁按鈕時，遠端指令會靜默消失且不會補送。現在改成由某一個分頁明確持有遠端遙控，切換畫面不影響，側邊欄該分頁的圖示右上角會有一個綠點標示。同時開兩個分頁的遠端遙控也不會再讓同一則指令被執行兩次。

## v1.8.0

**隨時看得到訂閱方案還剩多少額度**
選擇模型的按鈕旁邊多了一個小長條與數字，顯示目前這個供應商的額度用掉多少。Claude 訂閱會顯示 5 小時或 7 天窗口的使用百分比（自動挑比較吃緊的那個），GitHub Copilot 顯示 premium 額度的剩餘次數，ChatGPT 訂閱顯示目前計費週期的用量。快用完時長條會轉成琥珀色、再滿一點變紅色，平常則保持低調不干擾。滑鼠移上去可以看到完整資訊，包含還有多久重置。

終端機、Ask AI 面板、設計與規格、知識庫、資料庫、版本控制等畫面都看得到，不必特地切到設定頁去查。使用本地模型（Ollama、自架端點）時不會顯示——那些本來就沒有額度限制。

**新增「訂閱額度」設定頁**
設定裡多了一頁，一次列出所有供應商的額度狀態，每個時間窗都有進度條，並可手動重新整理。

**開始記錄自己的 token 用量**
同一頁下方會顯示 AITerm 自己用掉多少 token，可以依今天／7 天／30 天切換，並依供應商與模型分開統計次數、輸入、輸出與估算成本。查得到單價的模型才會顯示金額，查不到的顯示「—」而不是假的 $0。

**AI 代理任務進行中會顯示累計 token**
多步驟的自動任務最容易不知不覺用掉大量額度，現在執行中就看得到本次已經用了多少，不必等跑完才發現。

## v1.7.13

**版本控制的 SVN 連線更可靠了**
先前如果是先開啟 AITerm、之後才另外安裝 SVN，或是內部伺服器用的是自簽/公司內部憑證，「測試連線」都會失敗且訊息不夠明確。現在能自動找到剛裝好的 SVN、能正確信任內部憑證；編輯既有連線時就算密碼欄位留白（代表「不更新密碼」），測試連線也能正常運作，不會誤判成認證失敗。

**版本控制的 AI 代理不會再卡死**
執行多步驟查詢時，如果 AI 沒有在合理時間內回應，畫面會停在「執行中」動彈不得，按停止也沒有用。現在逾時會自動中止，停止按鈕也能真正中斷還在進行的請求。

**執行中的圖示改成真的會轉動**
版本控制畫面顯示「執行中」時的圖示，之前是靜止的，現在會轉動。

## v1.7.12

**本版包含 v1.7.10 與 v1.7.11 的內容**（那兩版未發布安裝檔）。

**AI 回覆不再卡住不動**
以前只要 AI 需要動用工具（MCP、程式庫協助的檔案搜尋），畫面會停頓好幾秒才把整段答案一次貼出來。現在文字會逐字出現，看得到它正在寫。所有 AI 供應商都適用。

**Claude 訂閱帳號可以搭配 MCP 工具了**
先前用 Claude 訂閱（非 API Key）開啟 MCP 會失敗，錯誤訊息說用量不足，但帳號用量其實還很充裕——那類請求會被 Anthropic 算到另一個計費項目。現在會自動切換方案讓工具照常運作，不需要另外儲值，畫面上也會說明目前的狀況與如何改回原生模式。

**Claude Code 橋接可以搭配 Anthropic 訂閱模型了**
先前選 Anthropic 的訂閱模型會連續跳出 400 錯誤而無法對話，現在正常。

**錯誤訊息不再誤導**
- 憑證讀不到時（例如作業系統的鑰匙圈拒絕存取），原本顯示「尚未設定 AI Provider」，會讓人以為要重新設定——其實設定是好的。現在會說明是讀不到憑證，並提示在授權視窗選「一律允許」。
- 沒有可用的憑證儲存區時（部分 Linux 環境）仍顯示「尚未設定」，因為那種情況確實還沒設定過。

**程式庫協助的回答變乾淨**
不再把 AI 的內部工具指令（`<tool_call>`、`<function=…>`）混進答案裡。

## v1.7.9

**Agent 模式的回覆逐字顯示**
以前送出後要等一段時間才整段跳出來，現在文字會逐字出現。串流過程中不再露出 AI 準備執行的指令，避免誤點成執行兩次。

**AI 面板可以放大**
新增放大按鈕，面板可擴成滿版，長回覆讀起來不再擠。

**輸入框上方新增模式說明**
一行字告訴你現在送出會發生什麼：AI 只建議指令、自動執行、或呼叫 MCP 工具。

**修正 MCP 工具卡片被壓成一條細線**
對話變長時工具卡片會被擠扁，現在維持正常高度。
